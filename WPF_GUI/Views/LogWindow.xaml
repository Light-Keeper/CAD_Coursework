<Window x:Class="WPF_GUI.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d"
        Width="600" Height="300" MinHeight="60"
        Title="CAD Course Work - Log Viewer"
        WindowStartupLocation="CenterScreen">
    <ScrollViewer>
    <TextBox Name="LogViewer" Background="Black" Foreground="Azure" IsReadOnly="True"
             TextWrapping="Wrap" BorderThickness="0"
             Text="{Binding Path=AllLogMessages, Mode=OneWay}"/>
    </ScrollViewer>
</Window>
